@using ExamSystem.Models.TableModel
@using ExamSystem.Comm.CommHelper
@model List<stuScore>
@{
    ViewBag.Title = "成绩查询";
    Layout = "~/Views/Shared/_LayoutOtherMaster.cshtml";
}
<div class="x-nav">
    <a class="layui-btn layui-btn-small" style="line-height:1.6em;margin-top:3px;float:right" onclick="location.reload()" title="刷新">
        <i class="layui-icon layui-icon-refresh" style="line-height:30px"></i>
    </a>
</div>
<div class="layui-fluid">
    <div class="layui-row layui-col-space15">
        <div class="layui-col-md12">
            <div class="layui-card">
                <div class="layui-card-body " style=" text-align: center; font-weight: bold; color: #5a7c86; font-size: 18px;letter-spacing: 44px;">
                    <h1 style="margin-top: 17px; font-weight: bold;">我的成绩表</h1>
                </div>
                <div class="layui-card-body layui-table-body layui-table-main" style="width: 50%; margin: auto; ">
                    <table class="layui-table layui-form" align="center">
                        <thead>
                            <tr>
                                <th style="font-size: 18px;">所考试卷名称</th>
                                <th style="font-size: 18px;">教师姓名</th>
                                <th style="font-size: 18px;">我的成绩</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (stuScore score in Model)
                            {
                                <tr>
                                    <td style="font-size: 15px;">@score.examName </td>
                                    <td style="font-size: 15px;">@score.teachername </td>
                                    <td style="font-size: 15px; color: red;">@score.isScore 分</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
                <div class="layui-card-body ">
                    <div class="page">
                        <div>
                            @Html.OutPutPage((int)ViewBag.NowSize, (int)ViewBag.PageSize, (int)ViewBag.TotalCount)
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>






